# AI 求职智能助手 - 产品需求文档（PRD）

> **文档版本**：V2.0（基于 MVP 上线版本）
> **撰写人**：杨佳奇（独立产品负责人）
> **最后更新**：2026-08-06
> **在线 Demo**：https://ai-resume-assistant-so11.onrender.com

---

## 一、产品背景与定位

### 1.1 需求洞察

在求职过程中，应届生普遍面临一个核心痛点：**简历投递后石沉大海，却不知道问题出在哪里**。传统的简历优化服务存在三个明显短板：

| 现有方案 | 核心问题 |
|---------|---------|
| 求职辅导机构 | 价格高（200-800 元/次），反馈周期长（3-5 天） |
| 通用 AI 工具（如 ChatGPT） | 缺乏岗位针对性，建议笼统，无法量化匹配度 |
| 模板类网站 | 只解决格式问题，不解决内容与岗位的匹配问题 |

### 1.2 产品定位

**AI 求职智能助手**是一款基于 AI Agent 的简历分析工具，通过对比「简历内容」与「目标岗位 JD」，为求职者提供**量化匹配度评估 + 针对性优化建议**，帮助求职者在投递前发现问题、优化表达，提升简历通过率。

### 1.3 核心价值

- **对用户**：低成本（免费）、即时反馈（1 分钟内）、岗位针对性强的简历诊断
- **对产品经理（本人）**：完整的 AI 产品闭环实践，验证 AI Agent 在求职场景的落地可行性

---

## 二、场景与用户

### 2.1 核心使用场景

**场景描述**：

> 杨同学是一名软件工程专业的应届生，想应聘 AI 产品经理岗位。他有一份简历，但不确定自己的经历是否匹配岗位要求、表达是否专业。他在投递前打开 AI 求职智能助手，上传简历 PDF，粘贴目标岗位的 JD，1 分钟后获得一份包含匹配度评分、优劣势分析和具体优化建议的报告。

**场景三要素**：

- **何时用**：投递简历前（准备阶段）
- **为何用**：不确定简历是否匹配岗位，需要专业反馈
- **如何用**：上传简历 + 粘贴 JD → 获取分析报告 → 按建议优化简历

### 2.2 目标用户画像

| 维度 | 描述 |
|------|------|
| **核心用户** | 应届生求职者（本科/硕士） |
| **用户特征** | 缺乏简历优化经验、对岗位匹配度认知模糊、预算有限 |
| **核心诉求** | 快速了解简历与岗位的匹配情况，获得可执行的优化建议 |
| **使用频率** | 求职期间中高频使用（每次投递新岗位前使用一次） |

### 2.3 用户痛点与对应方案

| 用户痛点 | 产品解决方案 |
|---------|-------------|
| "不知道简历哪里不行" | 多维度匹配度评估（能力/经验/风险）+ 量化依据 |
| "不知道怎么改" | 提供修改前后对比（原文→建议表达）+ 改写依据 |
| "不知道该突出什么" | 优势识别 + 建议突出经历清单 |
| "怕 AI 乱编内容" | 所有建议必须引用简历原文，禁止虚构（Prompt 约束） |

---

## 三、核心流程

### 3.1 核心业务流程

```
用户上传简历（PDF/文本）
        ↓
用户输入目标岗位 JD
        ↓
【前端】JD 质量校验（字数/中文占比/关键词）
        ↓  校验失败 → 提示用户补充有效 JD
【后端】PDF 文本解析 → 提取简历内容
        ↓
【AI Agent】调用 Coze API，发送「简历 + JD + 分析指令」
        ↓
【AI Agent】流式返回结构化分析结果（JSON）
        ↓
【后端】解析 JSON + 数据清洗 + 字段补全
        ↓
【前端】渲染分析报告（匹配度/依据/优劣势/优化建议）
        ↓
用户根据建议优化简历
```

### 3.2 关键流程节点说明

| 节点 | 关键逻辑 | 设计考量 |
|------|---------|---------|
| JD 质量校验 | 校验中文字符占比≥30%、数字占比≤70%、含岗位关键词 | 避免无效输入浪费 AI 调用成本 |
| PDF 解析 | 使用 pdf-parse 提取文本 | 支持 PDF 格式，覆盖 90% 简历场景 |
| AI 分析 | Coze V3 流式响应，实时拼接内容 | 流式提升用户体验，避免长时间等待 |
| 数据清洗 | 过滤异常值（JSON 字符串、空值、格式错误） | 保证前端渲染稳定性 |
| 结果渲染 | 数据来源标识（实时/部分/示例） | 让用户明确知道当前数据来源，建立信任 |

---

## 四、功能需求

### 4.1 核心功能列表

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|--------|
| 简历输入 | 支持 PDF 上传或文本粘贴两种方式 | P0 |
| JD 输入 | 支持文本输入 + 示例 JD 快速填充 | P0 |
| JD 质量校验 | 拦截无效输入（纯数字、无意义内容） | P0 |
| AI 匹配分析 | 多维度量化匹配度（能力/经验/风险） | P0 |
| 分析依据展示 | 每条结论附带简历原文引用 + JD 对应要求 | P0 |
| 优化建议 | 提供修改前后对比 + 改写依据 | P0 |
| 数据来源标识 | 区分实时数据/部分数据/示例数据 | P1 |
| 健康检查接口 | 提供服务状态监控（用于保活） | P1 |

### 4.2 分析结果结构（核心输出）

AI 返回的结构化数据包含 5 个核心模块：

1. **匹配度评估**：整体匹配度 + 能力/经验/风险三个子维度
2. **分析依据**：按维度拆解，每条匹配/差距项附带简历原文引用
3. **优势清单**：识别简历中的核心亮点
4. **不足清单**：指出简历与岗位的差距点
5. **优化建议**：包含「建议突出」「建议补充」「表达优化」三类建议，其中表达优化提供修改前后对比

---

## 五、AI 能力设计

> 这是产品的核心模块，决定了分析结果的质量。

### 5.1 Agent 架构

```
用户输入（简历 + JD）
        ↓
   【Prompt 模板组装】
   - 角色：专业简历分析师
   - 任务：匹配度分析
   - 输入：简历内容 + 岗位 JD
   - 输出约束：严格 JSON 格式
        ↓
   【Coze Agent 调用】
   - 模型：Coze 大模型
   - 流式响应：实时返回
        ↓
   【后端解析与清洗】
   - JSON 提取（代码块优先）
   - 字段补全（缺失字段填默认值）
   - 脏数据过滤（异常字符串过滤）
        ↓
   结构化分析结果
```

### 5.2 Prompt 设计要点

Prompt 设计是 AI 产品经理的核心能力体现，本产品的 Prompt 重点约束以下 5 点：

| 约束项 | 设计目的 | Prompt 策略 |
|-------|---------|------------|
| **格式约束** | 确保返回可解析的 JSON | 要求用 \`\`\`json 代码块包裹 |
| **真实性约束** | 禁止 AI 编造简历内容 | 明确要求「原文引用必须摘录自简历，不得编造」 |
| **可解释性约束** | 每条建议必须有依据 | 要求「改写依据 = 简历原文 + JD 要求 + 改写逻辑」 |
| **结构化约束** | 保证字段完整 | 提供完整的 JSON 示例作为参考 |
| **字符安全约束** | 避免 JSON 解析失败 | 要求字符串值中不含双引号、大括号等特殊字符 |

### 5.3 流式响应处理策略

为提升用户体验，采用 Coze V3 流式响应 API，并设计三级容错机制：

| 机制 | 触发条件 | 处理方式 |
|------|---------|---------|
| **正常接收** | 流式数据完整接收 | 解析完整 JSON |
| **空闲超时** | 15 秒无新数据 | 判定接收完成，解析已有内容 |
| **总超时保护** | 90 秒未完成 | 返回已接收的部分内容 + 提示标识 |

---

## 六、异常处理

### 6.1 异常场景与处理方案

| 异常场景 | 触发条件 | 处理方案 | 用户感知 |
|---------|---------|---------|---------|
| **JD 无效** | 纯数字/无关键词/中文占比<30% | 前端拦截 + 后端二次校验 | 明确提示「请输入有效的岗位描述」 |
| **PDF 解析失败** | 文件损坏或非标准 PDF | 捕获异常，返回错误提示 | 提示「PDF 解析失败，请检查文件」 |
| **AI 调用超时** | 90 秒未响应 | 返回部分结果 + 数据来源标识 | 显示「部分分析结果」徽章 |
| **AI 返回格式错误** | JSON 解析失败 | 三级降级解析策略 | 透明处理，用户无感 |
| **AI 服务不可用** | API Key 未配置/网络异常 | 返回示例数据 + 警告标识 | 显示「示例数据」徽章 |
| **端口占用** | 本地开发端口冲突 | 友好提示 + 解决方案 | 显示具体解决命令 |

### 6.2 数据来源透明机制

为建立用户信任，所有分析结果都会明确标识数据来源：

| 标识类型 | 含义 | 触发条件 |
|---------|------|---------|
| **实时数据** | AI 完整分析结果 | Coze API 正常返回 |
| **部分数据** | AI 响应不完整 | 超时或空闲中断 |
| **示例数据** | 模拟数据 | API 不可用或输入无效 |

---

## 七、评估指标

### 7.1 AI 效果评估维度

作为 AI 产品，需要建立客观的评估体系来衡量 AI 输出质量：

| 评估维度 | 评估指标 | 评估方法 |
|---------|---------|---------|
| **匹配度准确性** | 匹配度评分与人工评估的一致性 | 选取 20 份简历，对比 AI 评分与 HR 评分 |
| **问题识别完整性** | 简历核心问题的覆盖率 | 人工标注简历问题，检查 AI 是否识别到 |
| **建议可执行性** | 用户能直接采纳的建议占比 | 用户测试，统计「可直接执行」的建议比例 |
| **原文引用准确率** | 引用内容是否真实存在于简历 | 自动校验引用文本是否在原文中 |
| **格式稳定性** | JSON 解析成功率 | 统计 100 次调用中成功解析的比例 |

### 7.2 产品体验评估指标

| 指标 | 目标值 | 监测方式 |
|------|--------|---------|
| 响应时间 | 单次分析 ≤ 30 秒 | 后端日志统计 |
| 分析成功率 | ≥ 95% | 成功调用数 / 总请求数 |
| 用户满意度 | ≥ 80% | 用户反馈收集 |
| 有效输入率 | ≥ 90% | 通过校验的请求 / 总请求 |

### 7.3 MVP 阶段验证结论

基于真实用户简历的端到端测试，核心验证结论：

- ✅ AI 能准确识别简历与岗位的匹配点与差距点
- ✅ 优化建议具有可执行性，用户可直接采纳
- ✅ 流式响应 + 容错机制保证了稳定的用户体验
- ⚠️ 对于非标准格式简历（如纯图片 PDF），解析能力有限，需后续扩展 OCR 能力

---

## 附录：技术架构

```
前端（HTML + CSS + JavaScript）
    ↓ fetch('/api/analyze')
后端（Node.js + Express）
    ├── PDF 文本解析（pdf-parse）
    ├── JD 质量校验（字符占比 + 关键词匹配）
    ├── Coze Agent API 调用（V3 流式响应）
    └── 响应解析与数据清洗
    ↓
部署：Render（Web Service）+ cron-job.org（保活）
```

---

> **产品文档结束**
> 本文档基于 MVP 实际开发与上线版本编写，所有功能均已实现并验证。
> 在线体验：https://ai-resume-assistant-so11.onrender.com
